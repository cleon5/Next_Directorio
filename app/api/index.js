import { connectDb } from "../utils/mongoose";
//import UserSchema from "../models/directory2";
import { NextResponse } from "next/server";

//Get user with id
/*
export async function GetUser(id) {
    await connectDb();
    const data = await UserSchema.findById(id);
    console.log(data);
    return ( data );
}*/
